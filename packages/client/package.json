{
  "name": "client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "入站面客户端库：connect / submit / command / 收 event",
  "files": [
    "package.json",
    "README.md",
    "index.ts",
    "frame.ts",
    "protocol.ts",
    "socket.ts",
    "tsconfig.json"
  ],
  "engines": { "node": ">=24" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
