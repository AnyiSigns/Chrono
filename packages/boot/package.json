{
  "name": "boot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "CLI 薄壳（genesis 常量）：start / 客户端命令 / 离线命令",
  "files": [
    "package.json",
    "README.md",
    "main.ts",
    "commands.ts",
    "tsconfig.json"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
