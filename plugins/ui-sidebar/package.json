{
  "name": "ui-sidebar",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "侧边栏子应用：工作区分组 + 会话列表 + 会话管理 + 状态角标；slot 应用，自带服务与命令入口 term",
  "files": [
    "plugin.json",
    "package.json",
    "package-lock.json",
    "README.md",
    ".worldignore",
    "tsconfig.json",
    "execute/",
    "terms/"
  ],
  "engines": { "node": ">=24" },
  "scripts": {
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
