{
  "name": "ui-threads",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "线程顶栏：常显的线程标签（对话 / 子代理 / 群聊 / 工作流）+ 待办清单面板，点击切换当前视图线程",
  "files": [
    "plugin.json",
    "package.json",
    "package-lock.json",
    "README.md",
    ".worldignore",
    "execute/",
    "terms/"
  ],
  "engines": { "node": ">=24" },
  "scripts": {
    "build": "node execute/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
