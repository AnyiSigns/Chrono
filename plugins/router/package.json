{
  "name": "router",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "模型 / 能力选择（选别名端口名）：纯判定 select，无 pins、无模型调用、不读投影",
  "files": [
    "plugin.json",
    "package.json",
    "README.md",
    ".worldignore",
    "execute/",
    "schema/"
  ],
  "engines": { "node": ">=24" },
  "scripts": {
    "test": "node --test"
  }
}
