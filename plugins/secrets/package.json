{
  "name": "secrets",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "唯一密钥面：把 auth_ref 解析成本地文件 / 进程环境里的明文值（仅存调用方内存），并回本地存储清单",
  "files": [
    "plugin.json",
    "package.json",
    "README.md",
    ".worldignore",
    "execute/",
    "schema/"
  ],
  "engines": { "node": ">=24" },
  "scripts": {
    "test": "node --test"
  }
}
