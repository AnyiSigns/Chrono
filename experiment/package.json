{
  "name": "experiment",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "规模墙 bench + 150 行宿主闭环：对内核「源码以数据形式进出」决策的两个限时验证",
  "scripts": {
    "bench": "node --expose-gc --max-old-space-size=5632 bench-scale.ts",
    "bench:smoke": "node --expose-gc --max-old-space-size=2048 bench-scale.ts",
    "host": "node host.ts",
    "host:resume": "node host.ts resume",
    "host:boot": "node host.ts boot"
  }
}
